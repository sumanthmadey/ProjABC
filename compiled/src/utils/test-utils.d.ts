import { ElementFinder } from "protractor";
export declare function serveCompiled(entryPath?: string, port?: number): Promise<{}>;
export declare function hasClass(element: ElementFinder, cls: string): Promise<boolean>;
